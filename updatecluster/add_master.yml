---
# Playbook: Add Control Plane Master Node
# Usage: ansible-playbook -i inventory.ini updatecluster/add_master.yml --limit <new_master_hostname>

- name: Phase 0 - Bootstrap User (Dictator & SSH Key)
  hosts: rke2_servers
  become: true
  tags: bootstrap
  vars:
    target_user: dictator
  tasks:
    - name: Set OS-specific admin group fact
      ansible.builtin.set_fact:
        admin_groups: "{{ 'sudo,adm' if ansible_os_family == 'Debian' else 'wheel' }}"

    - name: Ensure target user exists
      ansible.builtin.user:
        name: "{{ target_user }}"
        shell: /bin/bash
        groups: "{{ admin_groups }}"
        append: true
        state: present
      ignore_errors: true

    - name: Configure Passwordless Sudo for {{ target_user }}
      ansible.builtin.copy:
        content: "{{ target_user }} ALL=(ALL) NOPASSWD: ALL"
        dest: "/etc/sudoers.d/{{ target_user }}"
        mode: '0440'
        validate: 'visudo -cf %s'

    - name: Set up authorized keys for {{ target_user }}
      ansible.posix.authorized_key:
        user: "{{ target_user }}"
        state: present
        key: "{{ lookup('file', '~/.ssh/id_ed25519.pub') }}"
      ignore_errors: true

- name: Phase 1 - Prepare New Master Node
  hosts: rke2_servers
  become: true
  tags: common
  roles:
    - common

- name: Phase 2 - Join New Master Node to Cluster
  hosts: rke2_servers
  become: true
  tags: join_server
  roles:
    - rke2_server_join

- name: Phase 3 - Verify Control Plane Health
  hosts: rke2_servers[0]
  become: true
  tasks:
    - name: Check Kubernetes node readiness
      ansible.builtin.shell: /var/lib/rancher/rke2/bin/kubectl get nodes -o wide
      register: nodes_out
      environment:
        KUBECONFIG: /etc/rancher/rke2/rke2.yaml

    - name: Display cluster node status
      ansible.builtin.debug:
        msg: "{{ nodes_out.stdout_lines }}"
